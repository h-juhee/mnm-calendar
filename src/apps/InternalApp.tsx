import ScheduleBuilderPage from '../components/ScheduleBuilderPage';

/** 내부 직원용 앱입니다. 기존 제작 화면을 그대로 유지합니다. */
export default function InternalApp() {
  return <ScheduleBuilderPage appMode="internal" />;
}
